# Keep backend and operational files local-only
private/

# Optional: keep only the public app in this repo
# (public/ remains tracked)

# Keep optional form config local-only
formConfig.js
public/formConfig.js
